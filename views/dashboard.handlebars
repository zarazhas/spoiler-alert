{{#if posts.length}}
<section>
    <h2>Posts written by you</h2>
        {{#each posts as |post|}}
            {{> dashboard-post post}}
        {{/each}}
</section>
{{/if}}

<button type="submit" class="btn">
    <a class="link" href="/dashboard/new">New Post</a>
</button>